# PRD Addendum — নতুন ফিচার (Add-New-Feature)

## প্রজেক্ট: SecuroFi.Tech

**Developer:** Ziaul Islam
**ডকুমেন্ট টাইপ:** Addendum (মূল PRD.md-এর পরিপূরক, এতে মূল PRD.md-এর কিছুই পরিবর্তন করা হয়নি)
**ডকুমেন্ট ভার্সন:** 1.2 (Updated — Auto Timezone Detection & Display যুক্ত)
**তারিখ:** September 2026

---

## ভূমিকা

এই ডকুমেন্টে শুধুমাত্র সেই ফিচারগুলো রাখা হয়েছে যা মূল **PRD.md**-তে এখনো নেই, কিন্তু প্রজেক্টের স্থিতিশীলতা, নিরাপত্তা, পারফরম্যান্স এবং AI/LLM-visibility-এর জন্য যোগ করা উচিত। এটি একটি **আলাদা, স্বতন্ত্র ফাইল** — মূল PRD.md-এ কোনো পরিবর্তন করা হয়নি। ডেভেলপমেন্টের সময় এই ফিচারগুলো **Phase 2 (Post-MVP)** হিসেবে বিবেচনা করা যেতে পারে।

---

## ১. কন্টেন্ট ডিস্ট্রিবিউশন ও AI/LLM ভিজিবিলিটি

### ১.১ RSS / Atom Feed
- সাইটের সব আর্টিকেলের জন্য অটো-জেনারেটেড RSS (`/feed`) এবং Atom ফিড।
- ক্যাটাগরি-ভিত্তিক আলাদা ফিড (যেমন `/category/cybersecurity/feed`)।
- অ্যাডমিন প্যানেল থেকে ফিড অন/অফ এবং কতগুলো সাম্প্রতিক পোস্ট দেখাবে তা কনফিগার করা যাবে।
- **কেন দরকার:** কন্টেন্ট সিন্ডিকেশন, RSS রিডার/অ্যাগ্রিগেটর এবং কিছু ক্রলার এখনো ফিড থেকে কন্টেন্ট ডিসকভার করে।
- **প্যাকেজ:** `spatie/laravel-feed` অথবা কাস্টম XML জেনারেটর।

### ১.২ llms.txt ফাইল
- সাইটের রুটে (`/llms.txt`) একটি স্ট্যান্ডার্ড ফাইল থাকবে, যা AI/LLM ক্রলার (ChatGPT, Claude, Perplexity ইত্যাদি)-কে সাইটের স্ট্রাকচার, মূল ক্যাটাগরি ও গুরুত্বপূর্ণ পেজের লিংক বুঝতে সাহায্য করবে।
- অ্যাডমিন প্যানেল থেকে এই ফাইলের কন্টেন্ট এডিট/রিজেনারেট করা যাবে (নতুন আর্টিকেল পাবলিশ হলে অটো-আপডেট অপশন সহ)।
- **কেন দরকার:** AEO (Answer Engine Optimization) — AI চ্যাটবটগুলো যেন সাইটের কন্টেন্টকে সহজে বুঝে ও রেফারেন্স/সাজেস্ট করতে পারে (যেটা মূল ভিডিওতেও উল্লেখ ছিল)।

---

## ২. মনিটরিং ও রিলায়েবিলিটি

### ২.১ Error Monitoring (Sentry / Bugsnag)
- প্রোডাকশন এনভায়রনমেন্টে রিয়েল-টাইম এরর ট্র্যাকিং ও অ্যালার্ট (ইমেইল/Slack নোটিফিকেশনসহ)।
- Laravel Exception Handler-এর সাথে ইন্টিগ্রেটেড, ইউজার-ফেসিং পেজে এরর দেখা যাওয়ার আগেই ডেভেলপার নোটিফাই হবে।
- **প্যাকেজ:** `sentry/sentry-laravel`

### ২.২ Uptime Monitoring
- বাইরের সার্ভিস (UptimeRobot / Better Uptime) দিয়ে সাইট প্রতি কয়েক মিনিটে চেক হবে, ডাউন হলে সাথে সাথে Email/SMS/Telegram অ্যালার্ট।
- অ্যাডমিন প্যানেলে সাম্প্রতিক Uptime স্ট্যাটাস উইজেট (Optional, API দিয়ে পুল করা)।
- **কেন দরকার:** সাইট ডাউন থাকলে সেল/ট্রাফিক মিস হওয়ার আগেই যেন জানা যায়।

### ২.৩ Cloud Backup (Offsite/S3-Compatible)
- শুধু লোকাল ডেটাবেজ ব্যাকআপ ডাউনলোড না, বরং অটোমেটিক শিডিউলড ব্যাকআপ যা S3-compatible স্টোরেজে (Amazon S3, Wasabi, Backblaze B2 ইত্যাদি) সংরক্ষিত হবে।
- ডেটাবেজ + Media ফাইল উভয়ই কভার করবে।
- রিটেনশন পলিসি (যেমন শেষ ৭ দিনের ডেইলি + শেষ ৪ সপ্তাহের উইকলি ব্যাকআপ রাখা)।
- **প্যাকেজ:** `spatie/laravel-backup`

---

## ৩. সিকিউরিটি (এক্সট্রা লেয়ার)

### ৩.১ reCAPTCHA / Cloudflare Turnstile
- Contact ফর্ম, নিউজলেটার সাবস্ক্রিপশন ফর্ম এবং (যদি থাকে) কমেন্ট ফর্মে বট/স্প্যাম প্রোটেকশন।
- অ্যাডমিন প্যানেল থেকে সাইট কী/সিক্রেট কী বসানো যাবে (কোনো কোড এডিট ছাড়া), প্রতিটি ফর্মে আলাদা অন/অফ টগল।

### ৩.২ Login History এক্সট্রা লেয়ার (Device Fingerprinting)
- (মূল PRD.md-এ বেসিক Login History আছে) — এর সাথে যোগ হবে সন্দেহজনক লগইন প্যাটার্নে অটো-ইমেইল অ্যালার্ট (নতুন ডিভাইস/লোকেশন থেকে লগইন হলে)।

---

## ৪. পারফরম্যান্স ও ডেলিভারি

### ৪.১ CDN ইন্টিগ্রেশন (Cloudflare)
- সব স্ট্যাটিক অ্যাসেট (ইমেজ, CSS, JS) CDN-এর মাধ্যমে সার্ভ হবে — গ্লোবাল অডিয়েন্সের জন্য লোড টাইম উল্লেখযোগ্যভাবে কমবে।
- Cloudflare-এর ফ্রি টিয়ার দিয়েই শুরু করা যাবে (DDoS প্রোটেকশন বোনাস হিসেবে পাওয়া যাবে)।
- অ্যাডমিন প্যানেল থেকে CDN Cache Purge বাটন (নতুন ডিজাইন/CSS আপডেট হলে ম্যানুয়ালি পার্জ করার জন্য)।

---

## ৫. কান্ট্রি-ভিত্তিক কন্টেন্ট ও অ্যাড টার্গেটিং (Geo-Targeting)

মূল PRD.md-এ ইতিমধ্যে `visitor_logs` টেবিলে ভিজিটরের Country ডেটা GeoIP দিয়ে ক্যাপচার করার প্ল্যান আছে (সেকশন ৫.২.১১)। সেই একই ডেটা ব্যবহার করে এখন কন্টেন্ট ও অ্যাড দুটোই দেশ-ভিত্তিক পার্সোনালাইজ করা যাবে — এটা একটা যৌক্তিক এক্সটেনশন, নতুন কোনো ভারী ইনফ্রাস্ট্রাকচার লাগবে না।

### ৫.১ Country-Based Content Visibility/Restriction
- প্রতিটি আর্টিকেল/পেজের জন্য অ্যাডমিন প্যানেল থেকে **Country Targeting** সেট করা যাবে: "সব দেশে দেখাও" / "শুধু নির্দিষ্ট দেশে দেখাও" / "নির্দিষ্ট দেশে লুকাও"।
- **ব্যবহারের উদাহরণ:** কোনো Personal Finance আর্টিকেল যদি শুধু US ট্যাক্স আইন নিয়ে হয়, সেটা শুধু US ভিজিটরদের দেখানো যাবে, আর বাংলাদেশ/ভারতের জন্য আলাদা লোকালাইজড ভার্সন দেখানো যাবে।
- একই আর্টিকেলের **Country-Specific Variant** তৈরি করার অপশন (একটা মাস্টার আর্টিকেল, কিন্তু একাধিক দেশ-ভিত্তিক ভ্যারিয়েন্ট লিংকড থাকবে — hreflang ট্যাগ অটো-জেনারেট হবে SEO-র জন্য)।
- Restricted কন্টেন্টের জন্য একটা ফলব্যাক পেজ/মেসেজ দেখানো যাবে (কোনো এরর পেজ না)।

### ৫.২ Country-Based Ad Targeting
- Ads Management Module (মূল PRD.md সেকশন ৫.২.৪)-এর প্রতিটি Ad Slot-এ এখন **Country Rule** যোগ হবে — একটা Ad Slot নির্দিষ্ট দেশে দেখানো, অন্য দেশে ভিন্ন Ad Network/কোড দেখানো, অথবা কিছু দেশে সম্পূর্ণ বন্ধ রাখা।
- **ব্যবহারের উদাহরণ:** ইউরোপের ভিজিটরদের জন্য GDPR-কমপ্লায়েন্ট Ad Network, US-এর জন্য AdSense, আর অন্য দেশে হয়তো ভিন্ন CPM-ভিত্তিক নেটওয়ার্ক — এভাবে দেশভেদে সবচেয়ে বেশি Revenue-generating অ্যাড সার্ভ করা যাবে।
- মাল্টিপল Ad Network কোড এক স্লটে দেশ অনুযায়ী প্রায়োরিটি অর্ডারে সেট করা যাবে (Fallback সহ, যদি কোনো দেশের জন্য নির্দিষ্ট কিছু সেট করা না থাকে)।

### ৫.৩ Country-Based Affiliate Link Switching
- (Addendum-এর আগের ড্রাফটে "Multi-Currency/Region Aware Affiliate Links" হিসেবে সংক্ষেপে উল্লেখ ছিল — এখানে এটাকে পূর্ণাঙ্গ ফিচার হিসেবে বিস্তারিত করা হলো।)
- একটা প্রোডাক্টের জন্য একাধিক দেশ-ভিত্তিক Affiliate লিংক সেট করা যাবে (যেমন amazon.com / amazon.co.uk / amazon.de / amazon.in) — ভিজিটরের দেশ অনুযায়ী সঠিক লিংকটা অটোমেটিক দেখানো হবে, যাতে কমিশন মিস না হয়।
- যেসব দেশে Amazon Affiliate প্রোগ্রাম কাজ করে না, সেখানে ফলব্যাক হিসেবে অন্য কোনো Affiliate নেটওয়ার্ক/লিংক দেখানোর অপশন।

### ৫.৪ ইমপ্লিমেন্টেশন নোট
- IP থেকে দেশ ডিটেকশনের জন্য মূল PRD.md-এ প্ল্যান করা `stevebauman/location` প্যাকেজই যথেষ্ট (নতুন প্যাকেজ লাগবে না)।
- Country-based rendering সার্ভার-সাইডে হবে (Middleware দিয়ে), যাতে ক্লায়েন্ট-সাইড ফ্ল্যাশ (ভুল অ্যাড/কন্টেন্ট প্রথমে দেখানো তারপর বদলানো) না হয়।
- Cache Strategy: Country অনুযায়ী পেজ ভিন্ন হলে সাধারণ ফুল-পেজ ক্যাশ কাজ করবে না — তাই Edge/CDN-লেভেলে Country-aware caching (Cloudflare Workers/Vary header) বিবেচনা করতে হবে, নাহলে পারফরম্যান্স কমে যেতে পারে।
### ৫.৫ অটো টাইমজোন ডিটেকশন ও ডিসপ্লে (Auto Timezone by Country)
- ভিজিটরের IP থেকে GeoIP-এর মাধ্যমে দেশ ডিটেক্ট হওয়ার সাথে সাথে সেই দেশের **টাইমজোনও** অটোমেটিক ডিটেক্ট হবে (দেশ → টাইমজোন ম্যাপিং টেবিল দিয়ে, অথবা ব্রাউজারের `Intl.DateTimeFormat().resolvedOptions().timeZone` ক্লায়েন্ট-সাইডে ব্যবহার করে আরও একিউরেট রেজাল্ট)।
- সাইটের সব জায়গায় (আর্টিকেল পাবলিশ ডেট, "৩ ঘণ্টা আগে" টাইপ রিলেটিভ টাইম, কমেন্টের সময়, Live Visitor Feed-এর টাইম) — ভিজিটর যেই দেশ থেকেই আসুন না কেন, সময়টা তার **নিজের লোকাল টাইমজোনে** দেখানো হবে, সার্ভার টাইমজোনে না।
- **অ্যাডমিন প্যানেল সেটিংস:**
  - অ্যাডমিন প্যানেলের নিজস্ব Default/Fallback Timezone সেট করা যাবে (Branding & Design Settings-এর মধ্যে, ৫.২.৯)।
  - অ্যাডমিন যখন নিজে ড্যাশবোর্ডে লগইন করবেন (Article Schedule, Analytics, Login History দেখার সময়), তখন তার নিজের প্রোফাইল থেকে পছন্দমতো টাইমজোন সিলেক্ট করতে পারবেন (Dropdown — যেমন "Asia/Dhaka", "America/New_York" ইত্যাদি) — এটা অ্যাডমিন প্যানেলে যা কিছু টাইম-স্ট্যাম্প দেখায় সবকিছুতে প্রযোজ্য হবে।
  - "Auto-Detect Visitor Timezone" টগল অন/অফ করা যাবে — অফ থাকলে সবার জন্য সাইট Default Timezone-এ সময় দেখাবে।
- **টেকনিক্যাল নোট:** ডেটাবেজে সব টাইমস্ট্যাম্প UTC-তে সংরক্ষিত হবে (Laravel-এর ডিফল্ট বিহেভিয়ার), এবং ডিসপ্লে করার সময় ভিজিটর/অ্যাডমিনের টাইমজোন অনুযায়ী কনভার্ট হবে (JS `Intl` API অথবা `carbon`-এর টাইমজোন কনভার্শন ব্যবহার করে) — এতে ডেটা ইন্টিগ্রিটি ঠিক থাকবে, শুধু ডিসপ্লে লেয়ারে কনভার্সন হবে।

---

## ৬. ডেভেলপমেন্ট ওয়ার্কফ্লো ও কোয়ালিটি অ্যাসুরেন্স

### ৬.১ CI/CD পাইপলাইন (GitHub Actions)
- কোড পুশ করলে অটোমেটিক টেস্ট রান + প্রোডাকশন/স্টেজিং সার্ভারে ডিপ্লয় হবে।
- ম্যানুয়াল FTP/SSH আপলোডের ঝুঁকি ও সময় দুটোই কমবে।
- ডিপ্লয়মেন্টের আগে অটোমেটিক `composer install`, `migrate`, `cache:clear` রান হবে।

### ৬.২ Staging Environment
- লাইভ (Production) সাইটের আলাদা একটি কপি (Staging) থাকবে, যেখানে নতুন ফিচার/থিম পরিবর্তন আগে টেস্ট করা যাবে — লাইভ সাইটে সরাসরি এক্সপেরিমেন্ট করার ঝুঁকি থাকবে না।
- Staging সাবডোমেইন (যেমন `staging.securofi.tech`) — search engine index-এ যাতে না আসে তার জন্য `noindex` + পাসওয়ার্ড প্রোটেকশন।

### ৬.৩ বেসিক Automated Testing (PHPUnit/Pest)
- ক্রিটিক্যাল ফিচারগুলোর (Authentication, Affiliate Link রেন্ডার, SEO Meta Tag আউটপুট, Contact ফর্ম সাবমিশন) জন্য বেসিক অটোমেটেড টেস্ট থাকবে।
- **কেন দরকার:** ভবিষ্যতে নতুন কোড যোগ করার সময় পুরনো ফিচার ভেঙে গেলে ডিপ্লয়মেন্টের আগেই ধরা পড়বে (ম্যানুয়ালি প্রতিবার পুরো সাইট চেক করার দরকার হবে না)।

---

## ৭. প্রায়োরিটি সাজেশন (এই Addendum-এর মধ্যেই)

সব একসাথে না করে নিচের ক্রমে করলে সবচেয়ে বেশি ভ্যালু কম সময়ে পাওয়া যাবে:

| অগ্রাধিকার | ফিচার | কারণ |
|---|---|---|
| High | Cloud Backup, reCAPTCHA | ডেটা লস ও স্প্যাম থেকে সরাসরি সুরক্ষা, সেটআপ সহজ |
| High | RSS Feed, llms.txt | কন্টেন্ট বানানোর সাথে সাথে যোগ করা সবচেয়ে সহজ, ট্রাফিক/AI ভিজিবিলিটিতে সরাসরি প্রভাব |
| Medium | CDN (Cloudflare), Uptime Monitoring | সাইট লাইভ হওয়ার পরপরই করলে ভালো, ফ্রি টিয়ারেই সম্ভব |
| Medium | Auto Timezone Detection (৫.৫) | ইমপ্লিমেন্ট করা তুলনামূলক সহজ, গ্লোবাল ইউজারের জন্য ইউজার-এক্সপেরিয়েন্স সরাসরি ভালো করে |
| Medium | Error Monitoring (Sentry) | ইউজার বেস বাড়ার সাথে সাথে গুরুত্ব বাড়ে |
| Medium-Low | Country-Based Ad/Affiliate Targeting (৫.২, ৫.৩) | রেভিনিউ অপটিমাইজেশনে সরাসরি প্রভাব আছে, কিন্তু আগে ভালো পরিমাণ গ্লোবাল ট্রাফিক থাকা দরকার — নাহলে ROI কম |
| Low | Country-Based Content Restriction (৫.১) | নিশ-স্পেসিফিক (যেমন US ট্যাক্স আর্টিকেল) কন্টেন্ট লেখা শুরু করলে তখন দরকার হবে, শুরুতে জরুরি না |
| Low (পরে) | CI/CD, Staging, Automated Tests | টিমে আরেকজন ডেভেলপার যোগ হলে বা কোডবেজ বড় হলে সবচেয়ে বেশি উপকারী — একজন ডেভেলপার হিসেবে শুরুতে এটা optional |

---

*এই ডকুমেন্টটি মূল PRD.md-এর কোনো অংশ পরিবর্তন করে না — এটি একটি স্বতন্ত্র সংযোজন (addendum) যা প্রয়োজনমতো আলাদাভাবে ইমপ্লিমেন্ট করা যাবে।*
